exports.parseOpts = function(e) {
  var view = e.data.args[1].name;
  
  return {
    content : var view = e.data.args[1].name,
    rand : 
  }
}

exports.applyFrame = function() {
  Math.random().toString().split(".").pop()
  // make frame html
  // insert into dom
}

exports.mustacheData = function(name) {
  return {
    name : name,
    rand : Math.random().toString().split(".").pop()
  };
}